import React from 'react';

export const UserIcon = ({color, style}) => (
    <i style={{...style, color}} className="fa fa-user"/>
);

export const ChannelIcon = ({color, style}) => (
    <i style={{...style, color}} className="fa fa-commenting"/>
);

